# Applied in tenant namespace cust-{id}
apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-quota
  namespace: cust-PLACEHOLDER_ID
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    persistentvolumeclaims: "2"
