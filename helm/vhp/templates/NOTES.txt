VHP Platform kuruldu.

Panel: https://{{ .Values.global.panelHost }}
( DNS ve Ingress hazır olduktan sonra erişilebilir. )

Kurulu zonlar:
  - core (ingress, gateways)
  - cert (Let's Encrypt)
  - control (controller)
  - panel
  - policy (Kyverno)
{{- if .Values.backup.enabled }}
  - backup (MinIO)
{{- end }}
{{- if .Values.data.enabled }}
  - data (PostgreSQL)
{{- end }}
{{- if .Values.dns.enabled }}
  - dns
{{- end }}
{{- if .Values.gitops.enabled }}
  - gitops (Flux)
{{- end }}
{{- if .Values.observability.enabled }}
  - observability
{{- end }}
{{- if .Values.build.enabled }}
  - build (Kaniko)
{{- end }}
{{- if .Values.cron.enabled }}
  - cron (CronJobs)
{{- end }}

Cert-manager ve Kyverno'yu cluster'a ayrıca kurmanız gerekebilir
(örn. helm install cert-manager, helm install kyverno).

Güncelleme: helm upgrade vhp ./helm/vhp -f values.yaml
